# Local development profile
# Copy this file to application-local.yaml and fill in your values:
#   cp application-local.yaml.example application-local.yaml

spring:
  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/freepdb1
    username: PDBADMIN
    password: Oracle123
    oracleucp:
      initial-pool-size: 2
      min-pool-size: 2
      max-pool-size: 5

  # Ollama defaults (http://localhost:11434, qwen2.5, nomic-embed-text) are
  # set in application.yaml. Override here only if needed:
  # ai:
  #   ollama:
  #     base-url: http://localhost:11434
  #     chat:
  #       model: qwen2.5
  #     embedding:
  #       model: nomic-embed-text

logging:
  level:
    dev.victormartin.agentmemory: DEBUG
    org.springframework.ai: DEBUG
